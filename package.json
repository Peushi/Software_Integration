{
  "name": "software-integration-app",
  "version": "1.0.0",
  "description": "NodeJS CRUD with dynamic PostgreSQL environments",
  "main": "src/app.js",
  "scripts": {
    "start:dev": "NODE_ENV=dev node src/app.js",
    "start:release": "NODE_ENV=release node src/app.js",
    "start:prod": "NODE_ENV=prod node src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}